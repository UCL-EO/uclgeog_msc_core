#!/bin/bash

rm -rf *egg*
rm -rf dist/uclcodebase*

python setup.py install
python setup.py sdist
anaconda upload --force dist/uclcodebase*.tar.gz

# update local
python setup.py install && bash postBuild
